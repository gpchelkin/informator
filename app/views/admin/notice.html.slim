= form_tag({controller: 'admin', action: 'reloadnotices'}, remote: true, id: "noticeReload")
  .small-12.columns
    button.small.radius.success name='commit' value='reloadnotices' data-disable-with=t('wait')
      = icon("download", t('reload_notice'))
br

#noticeTable
  == render 'noticetable'